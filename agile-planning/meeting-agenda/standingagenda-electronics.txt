PRAK Electronics Epic Decomposition — Standing Meeting

Purpose: Working sessions to evaluate & decompose the 15 Electronics PRAK epics into stories with time estimates and dependencies. Data collection only — artifacts are produced outside the meeting.

Scope: platform-team Electronics in prak-v-model (15 epics across 4 capabilities: CAP-01 Motion Authorization, CAP-03 Path Execution and Objectives Translation, CAP-08 Tele-operation, CAP-09 Observable Runtime State). Tracked separately from Embedded-Core; the two share parent PRD Capability Requirements but no epics.

Roles (RACI) — confirm before first session; Embedded roles do not carry over automatically:
• Estimation & decomposition (data): Accountable — Patrick McKee (owns tracker + outputs; editor). Responsible — TBD (Electronics SME: estimates, story breakdown, owner assignment; editor). Consulted — TBD (capacity/feasibility). Informed — Clint Jones.
• 2TS scope & priority (decision): Accountable — Clint Jones (PRAK PM owns 2TS scope). Consulted — TBD. Responsible — Patrick McKee (facilitate + record). Informed — TBD.

Estimation unit: all estimates in DAYS — keeps the tracker's Epic-total roll-up consistent (no story points).

Tracker (live — we capture directly here): TBD — the Electronics tracker does not exist yet. Until it does, the DAG is generated from prak-v-model.

Jira: the 15 epics are project ET (Electrical Team), keys ET-2951 through ET-2965. Their Jira parents are the shared PRD Capability Requirements (MCHTRNCS-259 Motion Authorization, MCHTRNCS-261 Path Execution and Objectives Translation, MCHTRNCS-265 Tele-operation, MCHTRNCS-266 Observable Runtime State) — the same parents the Embedded epics hang off. Note ET, not MCHTRNCS: a Jira search scoped to the Embedded project will not find these.

Epic Evaluation Loop (~4 min/epic):
1. System requirement clear after reading the epic? If not, mark needs-info and move on.
2. Required for 2TS? If yes, set priority rank.
3. How many stories to deliver this epic? Capture count + story titles.
4. Estimate time per story in DAYS.
5. Roll-up (auto = story count × days/story).
6. Blocking epics by ID — hard (can't start) vs soft (can start, can't finish). Blocking Epics resolves within THIS sheet only; a dependency on an Embedded-Core epic is recorded at the capability level, not here.

Agenda (15 epics fit in two sessions):
0:00–0:05  (1) Epic batch selection — confirm ~8 pre-selected epics; set target count.
0:05–0:10  (2) Carryover & blockers — resolve prior needs-info; add newly found dependencies (incl. cross-team to Embedded-Core).
0:10–0:45  (3) Evaluation loops — ~4 min/epic × 8.
0:45–0:53  (4) Assignment & uncertainty — assign epic owners; flag IC follow-ups.
0:53–1:00  (5) Loop evaluation — completion rate vs plan, adjust schedule, review assignments.

Output per meeting: ~8 epics with refined day estimates, story count/breakdown, and blocking-epic dependencies.
